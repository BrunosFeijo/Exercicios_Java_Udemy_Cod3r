// Pode ser homogênea
// Estrutura de Chave-Valor (Key-Value)
// Aceita valores duplicados
// Não aceita chaves duplicadas
// Não guarda ordem de inserção
// Pode ser indexado (usando a chave)

HashMap - Não guarda ordem, porém é mais eficiente
TreeMap - Ordena por chave ou conforme Comparable

put() = adiciona novos elementos // ou substitui os valores já existentes
putIfAbsent() = adiciona novos elementos // ou retorna o valor da chave já existente sem alterações
keySet() = lista chaves
values() = lista valores
entrySet() = lista ambos

containsKey() = procura chave / retorna boolean
containsValue() = procura valores / retorna boolean
get() = procura pela chave e retorna o valor
getOrDefault() = procura pela chave, se não encontrar, retorna o valor definido na implementação